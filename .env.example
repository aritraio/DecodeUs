# DecodeUs environment template — copy to .env.local
# Server-side only. NEVER prefix with NEXT_PUBLIC_.
GEMINI_API_KEY=
# Set MOCK_MODE=true for offline development without consuming Gemini quota
MOCK_MODE=true
NEXT_PUBLIC_APP_URL=http://localhost:3000
